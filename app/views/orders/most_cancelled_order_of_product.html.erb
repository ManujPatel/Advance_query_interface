<h2>top 5 customer who has Maximum cancelled orders:</h2>
<% Order.group(:customer_id).having(status:'cancelled').reverse_order.first(5).each do |a| %>
    <strong> Total no of cancelled order:</strong>
    <%= Order.where(customer_id:a.customer_id).where(status:'cancelled').count %>

    <strong>First Name:</strong>
    <%= a.customer.fname %>

    <strong>Last Name:</strong>
    <%= a.customer.lname %>

    <strong>Email:</strong>
    <%= a.customer.email%>

    <strong>Phone Number:</strong>
    <%= a.customer.phone_number %>
<% end %>
